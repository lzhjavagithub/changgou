package com.changgou.dao;

import com.changgou.goods.pojo.Brand;
import tk.mybatis.mapper.common.Mapper;

/**
 * insert
 * insertSelective()
 * update(T)
 * updateByPrimayKey(ID)
 * select(T)
 */
public interface BrandMapper extends Mapper<Brand> {
}
